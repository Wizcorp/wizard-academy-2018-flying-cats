export enum EffectObjectType {
	hitEffect = 0,
	itemGetEffect = 1,
	deathEffect = 2
}

//こうやって複数宣言できる。
//import { hoge,hoge2 } from ""という感じでインポートする

export enum EffectObjectTypeSample2 {
	hitEffect = 0,
	itemGetEffect = 1,
	deathEffect = 2
}

export enum EffectObjectTypeSample3 {
	hitEffect = 0,
	itemGetEffect = 1,
	deathEffect = 2
}